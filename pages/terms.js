export default function Terms() {
  return (
    <div style={styles.container}>
      <h1>利用規約</h1>

      <p>うさぎで仲介（以下「当サービス」）は…</p>

      <p>
        ※ここには前回作成した「利用規約全文」をそのまま貼り付けてください。
      </p>

    </div>
  );
}

const styles = {
  container: {
    maxWidth: "800px",
    margin: "0 auto",
    padding: "40px 20px",
    lineHeight: 1.8,
    fontFamily: "sans-serif",
  },
};
